{
  "name": "kenote-ssh-cli",
  "version": "1.0.0",
  "description": "own ssh command line tools ...",
  "main": "index.js",
  "preferGlobal": true,
  "bin": {
    "kssh": "./bin/index.js",
    "node-ssh": "./bin/index.js"
  },
  "scripts": {
    "test": "nyc --report-dir=\"coverage/cli\" mocha --reporter spec --bail --check-leaks tests/",
    "test-ci": "nyc --report-dir=\"coverage/cli\" mocha --reporter spec --check-leaks tests/",
    "report-coverage": "nyc report --reporter=lcov > coverage.lcov && codecov"
  },
  "dependencies": {
    "commander": "^2.12.2",
    "console.table": "^0.9.1",
    "fs-extra": "^5.0.0",
    "inquirer": "^4.0.2",
    "runscript": "^1.3.0"
  },
  "devDependencies": {
    "codecov": "^3.0.0",
    "mocha": "^4.1.0",
    "nyc": "^11.4.1"
  },
  "repository": "https://github.com/thondery/kenote-ssh-cli.git",
  "author": "thondery <thondery@163.com>",
  "license": "MIT"
}
